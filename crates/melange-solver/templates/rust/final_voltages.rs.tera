/// Compute final node voltages: v = v_pred + S * N_i^T * (i_nl - i_nl_prev)
#[inline(always)]
fn compute_final_voltages(
    v_pred: &[f64; N],
    i_nl: &[f64; M],
    state: &CircuitState,
) -> [f64; N] {
{% if m > 0 %}    // Compute current differences
{{ di_lines }}
{% endif %}    // Compute final voltages with correction term
    [
{{ voltage_lines }}    ]
}

